#include <stdio.h>

int main()
{
    // 变量声明
    int a[64], sum = 0;
    unsigned long long m;
    scanf("%d", &m);
    for (int i = 0; i < 64; i++)
    {
        a[i] = (m >> i) & (1) if (a[i] == 1)
        {
        }
    }

    return 0;
}